# Credenciales MySQL (usadas por docker-compose y por el backend en runtime).
# En local podés dejar los defaults. En producción overrideá con valores seguros.
DB_USER=examen
DB_PASSWORD=examen_pass
DB_NAME=examen3
